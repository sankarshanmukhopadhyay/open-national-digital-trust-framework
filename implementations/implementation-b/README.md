# Implementation B — independent-codebase simulation

**Status:** implementation evidence, not an external independent implementation.

This deliberately separate implementation consumes the published ONDTF interoperability record shape without importing or reusing `reference-implementation` business logic. It exists to test observable semantics and the interoperability harness. It does **not** satisfy the roadmap's requirement for independently authored external implementation evidence by itself.
