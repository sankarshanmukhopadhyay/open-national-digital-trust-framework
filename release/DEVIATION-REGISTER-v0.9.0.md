# v0.9.0 Candidate Deviation Register

The following are **evidence limitations, not hidden exceptions**:

1. External independently operated implementation evidence remains pending (`URI-01`).
2. Competent current jurisdiction/legal review remains pending (`URI-03`).
3. Quantitative baselines are provisional and internally derived (`URI-02`, `URI-13`).
4. Recognition evidence is an illustrative candidate method, not a legal recognition decision (`URI-16`).
5. External candidate-review participation is not fabricated; `REV-001` keeps this visible for v1.0.0 entrance review.
